# MEDIA PROMO

Static one-page marketing website ready for Vercel deployment.

## Local preview

```bash
npm run start
```

## Deploy to Vercel

```bash
npm run build
npx vercel
```

The site is static, so Vercel can also deploy it directly from the repository
without a custom build step.
# mediapromo
